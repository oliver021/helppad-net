namespace Helppad
{
    /// <summary>
    /// The stataegy task to implement a task in background
    /// </summary>
    public enum StrategyTask
    {
        Lineal,
        ThreadPool,
        Standalone
    }
}